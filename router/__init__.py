"""Router server (part 2): accepts the emulator's gateway TCP streams (protocol v1), validates them with the same
StreamChecker the emulator ships, and stores every patch's records in per-patch files.  See router/README.md."""
__version__ = "0.1.0"
